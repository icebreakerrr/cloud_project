apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  namespace: dev
  labels:
    app: ocr 
spec:
  selector:
    app: ocr
  ports:
    - protocol: TCP
      port: 25565
      targetPort: 25565
